#version 330 core
out vec4 FragColor;

in vec3 vertexPosition;
in vec3 ourColor;

void main()
{
    //FragColor = vec4(ourColor, 1.0f);
    //将片段的颜色设置为顶点的位置
    FragColor = vec4(vertexPosition, 1.0f);
}